### For 08-TDD demo, it is on the `main` branch

### For 08-TDD coverage demo, it is on the `TDD-coverage` branch

So you need to checkout the `TDD-branch` to see the coverage demo:
```
git checkout TDD-coverage
```

